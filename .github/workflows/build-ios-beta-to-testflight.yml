name: build-ios-beta-to-testflight
on:
  push:
    branches:
      - 'beta'
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Fastlane
        run: |
            gem install fastlane
      - name: Run Fastlane
        run: | 
          cd ios
          fastlane beta_ci
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
      - name: add test
        run: fastlane pilot add thomas_0836@qq.com -a thomas_0836@qq.com